import { FullSemesterPipe } from './full-semester.pipe';

describe('FullSemesterPipe', () => {
  it('create an instance', () => {
    const pipe = new FullSemesterPipe();
    expect(pipe).toBeTruthy();
  });
});
